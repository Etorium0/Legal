# HTTP Server Configuration
HTTP_PORT=8080

# Database Configuration
DATABASE_URL=postgres://legaluser:legalpass@localhost:5432/legaldb?sslmode=disable

# Auth Configuration
JWT_SECRET=dev-secret-change-me

# Embedding / RAG (optional)
EMBEDDING_ENABLED=true
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_API_KEY=

# QA (optional, uses same API key if OpenAI-compatible)
QA_MODEL=gpt-4o-mini

# Development Database (when using docker-compose)
# DATABASE_URL=postgres://legaluser:legalpass@postgres:5432/legaldb?sslmode=disable
